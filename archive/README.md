# Archived notes (historical, not current state)

This directory contains dated audits, plans, and other historical working notes.
Use these files only when a task explicitly asks about the relevant history or
artifact. Do not treat them as current product state or an active to-do list.

For current behavior and instructions, use the app's top-level `CLAUDE.md` and
the active documents outside this directory.
